# Gemfile for local development / CI builds
# Install with: bundle install
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
# If you plan to use remote themes locally (remote_theme), github-pages usually
# includes jekyll-remote-theme. If you need to add it explicitly, uncomment:
# gem "jekyll-remote-theme"
